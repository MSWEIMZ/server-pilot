# Server Pilot 项目规则

## 范围

- 本目录是 Server Pilot 的唯一源码与运行配置来源。
- 各 AI 工具中的安装目录应指向本目录，不维护独立代码副本。
- `scripts/server_config.json`、`scripts/known_hosts` 和 `C:\Users\WEI\server-pilot-archives\` 均属于本地敏感状态，不得提交、复制到文档或输出到回复。

## 修改与验证

- 修改前先读 `SKILL.md`、相关脚本和测试。
- SSH 连接必须校验已知主机密钥；不得恢复 `AutoAddPolicy`。
- 仪表盘默认只监听 `127.0.0.1`；远程模式必须显式启用并使用访问令牌。
- 不在脚本运行时自动安装依赖，不在日志中展示密码、Token、私钥或完整服务器配置。
- 修改 Python 后至少运行 `python -m unittest discover -s tests -v` 和 `python -m compileall -q scripts`。
- SSH 连接超时、通道读超时与远端命令本身必须分开：`--timeout` / `read_timeout` 只约束客户端读，默认不设限；不得把短读超时当作命令超时。
- 命令探测失败必须可见（`TimeoutError`/类型名非空），不得静默返回空字符串当作“无数据”。
- 修改迁移逻辑后还要验证所有安装目录仍解析到本源码库。

## 远端操作

- 只读状态检查可直接执行。
- 用户指定服务器、GPU、实验或 PID 时，只检查目标范围；只有用户要求全局资源概览或调度决策时才扫描全部服务器。
- 启动任务后必须回报服务器别名、GPU、PID/任务 ID、日志、工作目录、输出目录和查看命令。
- 服务器允许多人共享 GPU。启动当前任务前，检查目标 GPU 的实时剩余显存，并与任务预计峰值显存及安全余量比较；只要显存足够且评估不会 OOM，即使 GPU 利用率较高也可以并行启动。不得停止、重启或抢占非当前任务创建的进程；无法可靠确认显存安全时应换卡或等待。
- 进程消失不等于任务成功；必须结合日志、退出状态和预期产物判断。
- 分开保存远端任务退出码、调度 wrapper 退出码和科学 gate。wrapper 非零但远端任务退出码为 0 时先核对原因，不得自动判为训练失败。
- 运行观测允许 `UNKNOWN` 和 `BLOCKED`；连接失败不得解释为任务失败或资源空闲。

## Dashboard 数据口径

- `task_mgr/myjobs` 只代表本工具登记的任务，不代表服务器全部训练进程。
- 分开统计并命名：受管任务、检测到的训练进程、当前用户进程、全部用户进程、GPU 计算进程和 CPU 辅助进程。
- 全量状态应联合任务记录、操作系统进程树、GPU compute process 和日志，并按服务器与宿主 PID/进程身份去重；来源缺失或权限不足时显示 `UNKNOWN/coverage`，不得补猜。
- Dashboard 卡片和表格必须显示数据来源、统计范围和更新时间；不得把“所有人的进程”写成“我的任务”，也不得把 Jupyter、TensorBoard 或普通 CPU 进程自动计入训练数。

## 本地运行产物

- Dashboard 日志、临时编码文件、PID/状态文件和本机生成缓存不得提交；新增同类文件时先检查并更新 `.gitignore`，不擅自删除用户已有文件。
